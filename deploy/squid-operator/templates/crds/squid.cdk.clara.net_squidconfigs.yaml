---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: squidconfigs.squid.cdk.clara.net
spec:
  conversion:
    strategy: Webhook
    webhook:
      clientConfig:
        service:
          name: squid-operator-webhook-service
          namespace: squid-operator-system
          path: /convert
      conversionReviewVersions:
      - v1
  group: squid.cdk.clara.net
  names:
    kind: SquidConfigs
    listKind: SquidConfigsList
    plural: squidconfigs
    singular: squidconfigs
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.merged
      name: Merged
      type: boolean
    - jsonPath: .status.apply
      name: Applied
      type: boolean
    name: v1
    schema:
      openAPIV3Schema:
        description: SquidConfigs is the Schema for the squidconfigs API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: SquidConfigsSpec defines the desired state of SquidConfigs
            properties:
              rules:
                type: string
            required:
            - rules
            type: object
          status:
            description: SquidConfigsStatus defines the observed state of SquidConfigs
            properties:
              state:
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
