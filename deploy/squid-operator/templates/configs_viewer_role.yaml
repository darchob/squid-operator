# permissions for end users to view configs.
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    {{- include "squid-operator.labels" . | nindent 4 }}
  name: {{ include "squid-operator.fullname" . }}-configs-viewer-role
rules:
- apiGroups:
  - squid.cdk.clara.net
  resources:
  - configs
  verbs:
  - get
  - list
  - watch
- apiGroups:
  - squid.cdk.clara.net
  resources:
  - configs/status
  verbs:
  - get
