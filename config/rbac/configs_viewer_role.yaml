# permissions for end users to view configs.
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    app.kubernetes.io/name: squid-operator
    app.kubernetes.io/managed-by: kustomize
  name: configs-viewer-role
rules:
- apiGroups:
  - squid.cdk.clara.net
  resources:
  - configs
  verbs:
  - get
  - list
  - watch
- apiGroups:
  - squid.cdk.clara.net
  resources:
  - configs/status
  verbs:
  - get
